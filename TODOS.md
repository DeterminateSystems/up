# TODOs

- [ ] Support `disabled` field for process-compose processes
- [ ] Support quiet tasks in task runners
- [ ] Support leaving tasks out of `all` in task runners
- [ ] Remove status concept in task runner tasks
- [ ] Support both `alias` and `aliases` in tasks
- [ ] Support `command` and `commands` in benchmark tasks
